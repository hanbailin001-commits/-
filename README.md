# Project README

This repository contains an initial project skeleton. See docs for secrets & webhook setup.

## Quick links
- docs/SECRETS_AND_WEBHOOK.md — how to set repository secrets and webhook (ngrok + curl examples)
- backend/README_AUTH.md — backend auth endpoints and environment variables

